# ═══ Supabase (نسخ من لوحة Supabase → Settings → API) ═══
SUPABASE_URL=https://vsffmvrpuwhodgqpjzcg.supabase.co
SUPABASE_ANON_KEY=sb_publishable_...
SUPABASE_SERVICE_ROLE_KEY=sb_secret_...

# ═══ Server ═══
BACKEND_PORT=8000

# ═══ WhatsApp (رقم استقبال الطلبات — مع رمز الدولة) ═══
WA_PHARMACY_NUMBER=9705952224444

# ═══ Admin panel ═══
# ADMIN_PASSWORD_HASH: salt_hex$digest_hex بتقنية PBKDF2-HMAC-SHA256 (200k)
# لتوليده:  venv\Scripts\python.exe -c "from backend.app.security import hash_password; print(hash_password('كلمة سرك'))"
ADMIN_PASSWORD_HASH=
# سر توقيع جلسات الإدارة (32+ حرف عشوائي)
TOKEN_SECRET=
TOKEN_TTL_HOURS=24
# true إذا كان الخادم خلف HTTPS
COOKIE_SECURE=false